# PanditTalk

A multi-platform app (Flutter + Django) that connects users with Pandits for consultations, puja bookings, and horoscope services.

---

## 🧠 Tech Stack
- **Frontend:** Flutter (mobile + web)
- **Backend:** Django REST Framework
- **Database:** SQLite (dev) / PostgreSQL (prod)
- **Auth:** JWT & OTP login

---

## 🚀 How to Run Locally

### Backend
```bash
cd backend
pip install -r requirements.txt
python manage.py runserver

###Frontend (Flutter)
cd mobile
flutter pub get
flutter run