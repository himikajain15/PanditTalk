name: pandittalk_app
description: Pandittalk mobile app (Flutter) - modern saffron–gold themed version
publish_to: "none"
version: 1.1.0+1

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  animate_do: ^3.3.4

  # Core
  cupertino_icons: ^1.0.2
  
  http: ^1.1.0  
  flutter_secure_storage: ^8.0.0
  intl: ^0.20.2

  # Payments and real-time
  razorpay_flutter: ^1.3.8
  web_socket_channel: ^2.2.0

  # UI / Theming
  google_fonts: ^5.0.0
  flutter_svg: ^2.0.7
  animations: ^2.0.6
  image_picker: ^1.0.7
  
  # Localization
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

dependency_overrides:
  fluttertoast: ^9.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/logo.png
